// Runtime 63 ms Beats 41.14% || Memory 44 MB Beats 50.49%

class ArrayWrapper {
    constructor(arr){
        this.arr = arr
    }
    valueOf(){
        if( this.arr.length == 0 ) return 0
        return this.arr.reduce((t,c) => t + c)
    }
    toString(){
        return `[${this.arr.toString()}]`
    }
}
