var twoSum = function(nums, target) {
    for( let i = 0; i < nums.length; i++ ){
        if( nums.slice(i, i+2).reduce((total, current) => total + current) === target ){
            return [i, i + 1]
        }
    }
};
