// Runtime 61 ms Beats 39.63% || Memory 41.7 MB Beats 85.14%

var filter = function(arr, fn) {
    let newArr = []
    for( let i = 0; i < arr.length; i++ ){
        if( fn(arr[i], i) ){
            newArr.push(arr[i])
        }
    }
    return newArr
};
