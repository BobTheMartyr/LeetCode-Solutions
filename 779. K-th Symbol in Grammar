
// This kinda works...kinda
var kthGrammar = function(n, k) {
    if( n == 1 ) return 0
    let dict = {
        '0' : '01',
        '1' : '10'
    }
    let row = "0"
    while( n > 1 ){
        row = row.split('').map(el => dict[el]).join('')
        n--
    }
    return row[k-1]
};
