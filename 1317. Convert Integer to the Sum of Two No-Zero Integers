var getNoZeroIntegers = function(n) {
    
    let i = 1
    while( i < n ){
        if( (n - i) % 10 != 0 && !String(n-i).includes('0') && !String(i).includes('0') ){
            return [i, n - i]
        } else i++
    }
};
