// Runtime 62 ms Beats 34.4% || Memory 42.1 MB Beats 50.83%

var map = function(arr, fn) {
    let newArr = []
    for( let i = 0; i < arr.length; i++ ){
        newArr.push(fn(arr[i], i))
    }
    return newArr
};
